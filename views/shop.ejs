<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/css/main.css" />
    <style>
      /* Added for better card layout */

    </style>
  </head>
  <body class="body">
    <header class="main-header">
      <nav class="main-header__nav">
        <ul class="main-headr__item-list">
          <li class="main-headr__item"><a href="/" class="active">shop</a></li>
          <li class="main-headr__item">
            <a href="/admin/add-product">add-product</a>
          </li>
        </ul>
      </nav>
    </header>

    <main>
      <% if (prods.length > 0) { %>
        <div class="product-grid">
          <% for (let product of prods) { %>
            <div class="product-card-wrapper">
              <article class="card product-item">
                <header class="card__header">
                  <h1 class="product__title"><%= product.title %></h1>
                </header>
                <div class="card__image">
                  <img
                    src="<%= product.imageUrl || 'https://cdn.pixabay.com/photo/2024/04/29/04/21/neon-8726714_1280.jpg' %>"
                    alt="<%= product.title %>"
                  />
                </div>
                <div class="card__content">
                  <h2 class="product__price"><%= product.price || '400 L.E' %></h2>
                  <p class="product__description">
                    <%= product.description || 'Comfortable made with the best materials 100% Egyptian cotton' %>
                  </p>
                </div>
                <div class="card__actions">
                  <button class="btn btn-primary">Add to Cart</button>
                </div>
              </article>
            </div>
          <% } %>
        </div>
      <% } else { %>
        <h1 class="no-products">No Products Found...</h1>
      <% } %>
    </main>
  </body>
</html>